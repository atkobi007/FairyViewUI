<template>
	<view style="width: 100%;height: 100%;background-color: floralwhite;">
		<!-- <image class="logo" src="/static/logo.png"></image> -->
		<!-- <text class="title">{{title}}</text> -->
		<fui-badge>
			<fui-button type="success" left labelColor="#FFF" :text="title" :labelColor="labelColor"></fui-button>
		</fui-badge>
		<fui-gap></fui-gap>
		<fui-text></fui-text>
		<view style="display: flex;flex-direction: row; flex-wrap: wrap;align-items: center;">
			<fui-image />
			<fui-image width="200rpx" height="200rpx" :radius="imageRadius" />
			<fui-image width="300rpx" height="200rpx" />
		</view>
		<fui-icon :name="iconName" custom-font-family="/static/fui-icon.ttf" />
		<fui-icon />
		<!-- <view style="width:100%;height:240rpx;">
			<fui-swiper>
			</fui-swiper>
		</view> -->
		<fui-tabs></fui-tabs>
	</view>
</template>

<script setup>
	import { getBatteryInfoSync } from "../../uni_modules/fui-battery"
	import fuiState from "@/uni_modules/fairy-ui/fui-core/index.uts"
	
	const title = ref("test")
	const labelColor = ref("red")
	const imageRadius = ref(100)
	const iconName = "\ue9d9"
	onMounted(() => {
		// console.error("NVUE")
		// const result = getBatteryInfoSync()
		// console.error("fuiState:", getTheme("fui_background_olor"))
	})
</script>

<style>
	.box {
		width: 40px;
		height: 60px;
		display: flex;
		justify-content: center;
		align-items: center;
		text-align: center;
		background-color: #DCDFE6;
		color: #fff;
	}

	.box-text {
		text-align: center;
		color: #fff;
		font-size: 12px;
	}

	.logo {
		height: 100px;
		width: 100px;
		margin: 100px auto 25px auto;
	}

	.title {
		font-size: 28rpx;
		color: #8f8f94;
		text-align: center;
	}
</style>