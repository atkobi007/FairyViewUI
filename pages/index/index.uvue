<template>
	<view style="width: 100%;height: 100%;background-color: floralwhite;">
		<image class="logo" src="/static/logo.png"></image>
		<text class="title">{{title}}</text>
		<fui-badge>
			<fui-button :text="title" :labelColor="labelColor"></fui-button>
		</fui-badge>
		<fui-text></fui-text>
	</view>
</template>

<script setup>
	import { getBatteryInfoSync } from "../../uni_modules/fui-battery"
	
	const title = ref("test")
	const labelColor=ref("red")
	onMounted(() => {
		console.error("NVUE")
		const result = getBatteryInfoSync()
		console.error("sdk api:", result)
	})
</script>

<style>
	.box {
		width: 40px;
		height: 60px;
		display: flex;
		justify-content: center;
		align-items: center;
		text-align: center;
		background-color: #DCDFE6;
		color: #fff;
	}

	.box-text {
		text-align: center;
		color: #fff;
		font-size: 12px;
	}

	.logo {
		height: 100px;
		width: 100px;
		margin: 100px auto 25px auto;
	}

	.title {
		font-size: 28rpx;
		color: #8f8f94;
		text-align: center;
	}
</style>